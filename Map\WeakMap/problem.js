/* You want to keep track of how many times a method was called with a certian object 
and report if it happens more than N times. But you don't want to cause a memmory leak.
A very simple function has been provided below but please feel free to alter it.*/

let returnObj = obj => {

	console.log(obj);
}